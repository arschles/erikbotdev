apiVersion: v1
kind: Service
metadata:
  name: {{ include "erikbotserver.fullname" . }}
  labels:
    {{- include "erikbotserver.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "erikbotserver.selectorLabels" . | nindent 4 }}
